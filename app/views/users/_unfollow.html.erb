<%= form_for(current_user.active_follows.find_by(followed_id: @user.id),
            html: { method: :delete, id: @user.id },
            remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
